/// RYB 색상 모델에 따른 주 색상
#[derive(Debug)]
pub enum PrimaryColor {
    Red,
    Yellow,
    Blue,
}

/// RYB 색상 모델에 따른 보조 색상
#[derive(Debug)]
pub enum SecondaryColor {
    Orange,
    Green,
    Purple,
}
